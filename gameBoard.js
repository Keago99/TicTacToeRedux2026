


const gameboard = (() => {
    gameboard = new Array(9);


    //makes a move
    function makeMove(index, marker){
        // get the current players turn
        // get the current players symbol
        // check the index in the array is it null or is it occupied
        // if occupied flash the board once
        // if null put the symbol of the player here
    }

    // checks for winners
    function checkWinners(markerA, markerB){
        const winpatterns = [
            
        ]
    }
})();